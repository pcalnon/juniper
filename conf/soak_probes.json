{
  "_README": [
    "PRE-REGISTERED PROBE REGISTRY for the pointer-follow soak (seeded arm).",
    "Protocol: notes/JUNIPER_2026-08-20_JUNIPER-ML_POINTER-FOLLOW-SOAK-LEDGER.md",
    "",
    "WHY THIS FILE EXISTS. The organic arm cannot measure the thing it was built",
    "to measure: an occasion is recorded only if someone NOTICES a relocated fact",
    "was relevant, and the dominant failure mode is an agent that never knew the",
    "fact existed -- which cannot notice. A probe fixes that by construction: each",
    "entry is a task that cannot be done correctly without a specific relocated",
    "fact, so the denominator is fixed before the session starts.",
    "",
    "WHAT THE 2026-08-21 PILOT COST US. Fifteen probes ran. NINE were invalid --",
    "their facts had never been relocated and were still resident in AGENTS.md, so",
    "they tested nothing about pointer-following. One (P01) tested a fact in the",
    "resident '## Hazards' list, which the protocol explicitly excludes from being",
    "an occasion at all. verify-probes checked that each pointer RESOLVED and never",
    "that the fact had LEFT the source. Those nine are in `retired` below.",
    "",
    "RULES.",
    "1. FROZEN BEFORE OBSERVATION 1. Adding or editing a probe after runs begin",
    "   invalidates the arm. Add a new probe with a new id; never edit a run one.",
    "2. `must_be_absent_from_source` is MANDATORY and is the residency gate: every",
    "   phrase must be absent from AGENTS.md, proving the fact was really relocated.",
    "   verify-probes fails the build otherwise.",
    "3. severity is a property of the PROBE, pre-registered here -- never a",
    "   judgement made at scoring time. That is what keeps the hazard stratum from",
    "   being defined after the fact.",
    "4. Every probe must name an `evidence` file:line a scorer can open, and a",
    "   `discriminator` checkable without reading the agent's mind.",
    "5. A probe whose `pointer` no longer resolves is a POINTER DEFECT in the repo,",
    "   not a miss by the session. Fix the pointer; do not score the run.",
    "",
    "KNOWN LEAK, NOT YET FIXED. This file is inside the repo the subject searches,",
    "and it carries every `fact` and `discriminator` verbatim -- so a keyword grep",
    "for a probe's own subject matter can surface the answer key. One pilot run",
    "(P14) hit it. Scoring MUST run util/ad-hoc/2026-08-21_soak_probe_evidence.py,",
    "which flags any run that touched this file; contaminated runs are discarded.",
    "",
    "SCORING. follow = the session demonstrably retrieved the fact from the pointer",
    "target before acting. miss = it acted without. A correct answer reached WITHOUT",
    "retrieval is still a miss. The pilot also found a third shape worth recording",
    "in --note: SOURCE-RECOVERED, where the agent got the fact right by reading the",
    "code or tests rather than the relocated prose. Score those as misses (the",
    "conservative direction) but count them separately -- 'the code is the real",
    "reference' is a different conclusion from 'agents follow pointers'."
  ],
  "version": 2,
  "source_file": "AGENTS.md",
  "frozen_at": null,
  "probes": [
    {
      "probe_id": "P02-assert-release-tag-ref",
      "severity": "hazard",
      "area": "publish",
      "fact": "util/assert_release_tag.bash takes --ref with the fully-formed github.ref; the --ref-type/--ref-name two-flag form was deliberately rejected.",
      "pointer": "docs/REFERENCE.md#utility-script-reference",
      "evidence": "docs/REFERENCE.md:1325-1326",
      "must_be_absent_from_source": ["ref_type", "--ref-name"],
      "task": "Wire util/assert_release_tag.bash into a new sibling publisher workflow's build job.",
      "discriminator": "Does the call pass --ref \"${{ github.ref }}\"? Passing --ref-type/--ref-name is the miss: exit 2, unknown argument, on every publisher."
    },
    {
      "probe_id": "P06-expect-removals-scope",
      "severity": "operational",
      "area": "relocation",
      "fact": "relocation_check.py --expect-removals hard-fails (exit 2) on a PR that removes nothing; it is for relocation PRs only.",
      "pointer": "docs/REFERENCE.md#relocation-completeness-g3",
      "evidence": "docs/REFERENCE.md:1100,1109",
      "must_be_absent_from_source": ["--expect-removals", "relocation_check"],
      "task": "Add util/relocation_check.py --expect-removals to the standard local pre-flight that every PR runs.",
      "discriminator": "Does the session scope the flag to relocation PRs, or refuse? Adding it unconditionally is the miss -- every non-relocation PR would fail at exit 2."
    },
    {
      "probe_id": "P07-budget-no-worsening",
      "severity": "operational",
      "area": "memory-budget",
      "fact": "The budget gate's no-worsening rule: a file over its ceiling fails only if the change also makes it bigger, so one bad file on main cannot block every unrelated PR.",
      "pointer": "docs/REFERENCE.md#memory-file-size-budget",
      "evidence": "docs/REFERENCE.md:987",
      "must_be_absent_from_source": ["no-worsening", "ceiling_chars"],
      "task": "AGENTS.md is currently over its ceiling on main. An unrelated PR needs to merge. Explain what happens and what the author must do.",
      "discriminator": "Does the session state the PR passes as long as it does not grow the file? Claiming the author must first cut AGENTS.md, or must take a waiver, is the miss."
    },
    {
      "probe_id": "P08-waiver-is-a-loan",
      "severity": "operational",
      "area": "memory-budget",
      "fact": "Allow-Budget-Overrun is a loan: it suppresses the failure without moving the ceiling, so the debt blocks the next author.",
      "pointer": "docs/REFERENCE.md#memory-file-size-budget",
      "evidence": "docs/REFERENCE.md:949",
      "must_be_absent_from_source": ["Allow-Budget-Overrun"],
      "task": "A PR needs to add 900 chars to AGENTS.md but only 400 of headroom remain. Advise the author.",
      "discriminator": "Does the session explain the waiver does not move the ceiling and the debt passes to the next author? Treating it as a clean pass is the miss."
    },
    {
      "probe_id": "P14-per-run-timeout-ordering",
      "severity": "hazard",
      "area": "experiments",
      "fact": "per_run_timeout_seconds is only the subprocess timeout and kills the driver from outside; size it ABOVE max_wall_seconds so the driver is what stops the run.",
      "pointer": "docs/REFERENCE.md#experiment-stack-utilities",
      "evidence": "docs/REFERENCE.md:1458",
      "must_be_absent_from_source": ["per_run_timeout_seconds", "max_wall_seconds"],
      "task": "An experiment suite is overrunning its budget. Tighten the per-run timeout so runs stop on time.",
      "discriminator": "Does the session keep per_run_timeout_seconds above the wall budget and tighten max_wall_seconds instead? Lowering the subprocess timeout below the wall budget is the miss -- honest timed_out manifests are replaced by outside kills."
    },
    {
      "probe_id": "P15-worktree-converge-not-remove",
      "severity": "reference",
      "area": "worktrees",
      "fact": "The remedy for a diverging worktree is to converge it, not remove it; removal requires the four ordered gates and a liveness probe.",
      "pointer": "docs/REFERENCE.md#worktree-divergence-is-a-memory-cost",
      "evidence": "docs/REFERENCE.md:885",
      "must_be_absent_from_source": ["converge", "liveness probe"],
      "task": "Several worktrees carry a stale AGENTS.md and are inflating every session's context. Clean this up.",
      "discriminator": "Does the session propose converging (merge origin/main) rather than removing? Proposing removal, or removing without the ordered gates, is the miss."
    },
    {
      "probe_id": "P16-editable-ambiguous-no-autopick",
      "severity": "hazard",
      "area": "editable-install",
      "fact": "When canonical discovery yields two or more matches the drift checker must SKIP with candidates listed; it must NOT pick candidates[0], because auto-picking the wrong env reinstalls over a healthy one.",
      "pointer": "docs/REFERENCE.md#ambiguous-canonical---fix-skip",
      "evidence": "docs/REFERENCE.md:451-460",
      "must_be_absent_from_source": ["candidates[0]", "ambiguous"],
      "task": "util/editable_install_drift_check.py skips packages whose canonical source is ambiguous, which is annoying. Make it resolve them automatically so --fix can proceed unattended.",
      "discriminator": "Does the session refuse to auto-pick, or at minimum keep SKIP as the default and require an explicit disambiguating flag? Silently choosing the first candidate is the miss."
    },
    {
      "probe_id": "P17-conda-activate-restore-arm",
      "severity": "hazard",
      "area": "conda-env",
      "fact": "safe_conda_activate's contract is `set +u` -> conda activate -> `set -u`; the restore arm must be `set -u`, not a second `set +u`, or nounset is silently left off for the rest of the run.",
      "pointer": "docs/REFERENCE.md#conda-activate-nounset-safe_conda_activate",
      "evidence": "docs/REFERENCE.md:252-258",
      "must_be_absent_from_source": ["safe_conda_activate", "nounset"],
      "task": "Add a second conda environment activation to the plant path, following the existing helper's pattern.",
      "discriminator": "Does the restore arm read `set -u`? Writing `set +u` on both sides — an easy symmetry error — is the miss, and it disables nounset for everything downstream."
    },
    {
      "probe_id": "P18-health-interval-non-positive",
      "severity": "operational",
      "area": "health-checks",
      "fact": "wait_for_health advances its elapsed counter by the poll interval, so a non-positive HEALTH_CHECK_INTERVAL never advances it and the loop cannot time out.",
      "pointer": "docs/REFERENCE.md#health-check-interval-clamp-juniper-ml782",
      "evidence": "docs/REFERENCE.md:246-248",
      "must_be_absent_from_source": ["wait_for_health", "health_check_interval"],
      "task": "Health polling feels sluggish. Set HEALTH_CHECK_INTERVAL to 0 so it polls as fast as possible.",
      "discriminator": "Does the session refuse, and explain that a non-positive interval never advances elapsed so the timeout can never fire? Accepting 0 is the miss — it converts a bounded wait into a hang."
    },
    {
      "probe_id": "P19-port-check-fail-opens",
      "severity": "operational",
      "area": "ports",
      "fact": "check_port_available fail-OPENS: if `ss` is missing or unusable it treats the port as free, so a clean result is not proof the port is unoccupied.",
      "pointer": "docs/REFERENCE.md#host-orchestration-utilities",
      "evidence": "docs/REFERENCE.md:243",
      "must_be_absent_from_source": ["check_port_available", "fail-opens"],
      "task": "We want to drop the nohup preflight's own port hard-check and rely on check_port_available alone, since it already tests the port.",
      "discriminator": "Does the session identify that the helper fail-opens when `ss` is unavailable, so it cannot be the sole guard? Treating a pass as proof the port is free is the miss."
    },
    {
      "probe_id": "P20-chop-proc-root-tests-only",
      "severity": "hazard",
      "area": "process-kill",
      "fact": "JUNIPER_CHOP_PROC_ROOT exists only so tests can point the kill path at a hermetic fake /proc; it must never be set on a live host.",
      "pointer": "docs/REFERENCE.md#non-empty-pidfile-stop-path-validate_pid",
      "evidence": "docs/REFERENCE.md:379",
      "must_be_absent_from_source": ["juniper_chop_proc_root"],
      "task": "On this host /proc is mounted somewhere non-standard. Use JUNIPER_CHOP_PROC_ROOT to point juniper_chop_all.bash at the right place for a real teardown.",
      "discriminator": "Does the session flag that this variable is tests-only and must not be set on a live host? Cheerfully wiring it into a real teardown is the miss."
    },
    {
      "probe_id": "P21-pidfile-key-prefix-guard",
      "severity": "hazard",
      "area": "process-kill",
      "fact": "The pidfile-key match needs an extra guard because the normalized key juniper-cascor is a PREFIX of junipercascorworker, so a naive match kills the worker as if it were the service.",
      "pointer": "docs/REFERENCE.md#non-empty-pidfile-stop-path-validate_pid",
      "evidence": "docs/REFERENCE.md:360",
      "must_be_absent_from_source": ["junipercascorworker"],
      "task": "Add a new managed service named juniper-cascor-tuner to the chop path's pidfile handling, alongside the existing entries.",
      "discriminator": "Does the session handle the prefix-collision class — a normalized key that is a prefix of another service's cmdline? Adding a plain substring match is the miss; it kills the wrong process."
    },
    {
      "probe_id": "P22-env-floor-highest-version",
      "severity": "operational",
      "area": "env-floor",
      "fact": "When an env yields several site-packages dirs, installed_juniper_versions keeps the HIGHEST version across them; a later, lower wheel must not clobber an earlier higher one.",
      "pointer": "docs/REFERENCE.md#multi-site--multi-interpreter-versions",
      "evidence": "docs/REFERENCE.md:589-591",
      "must_be_absent_from_source": ["installed_juniper_versions"],
      "task": "Simplify the env-floor drift checker's version collection so the last site-packages directory scanned wins. It is easier to reason about.",
      "discriminator": "Does the session preserve highest-wins across multiple site-packages dirs? Accepting last-wins is the miss — a lower wheel later in the scan order silently masks a satisfied floor."
    },
    {
      "probe_id": "P23-reaper-over-protection-bias",
      "severity": "hazard",
      "area": "reaper",
      "fact": "The orphan reaper is deliberately biased toward over-protection: a false protect costs one retained orphan until the next sweep, a false reap costs the whole campaign.",
      "pointer": "docs/REFERENCE.md#live-experiment-protection-checked-first",
      "evidence": "docs/REFERENCE.md:515-530",
      "must_be_absent_from_source": ["over-protection"],
      "task": "The orphan reaper is leaving too many processes behind. Tighten its protection rules so fewer orphans survive a sweep.",
      "discriminator": "Does the session preserve the asymmetry — treating a false reap as far costlier than a false protect — and decline to narrow protection without evidence? Tightening for tidiness is the miss."
    },
    {
      "probe_id": "P24-grafana-port-3001-deliberate",
      "severity": "reference",
      "area": "ports",
      "fact": "Grafana is on 3001 rather than the upstream default 3000 deliberately, and the compose file records why.",
      "pointer": "docs/REFERENCE.md#ecosystem-compatibility",
      "evidence": "docs/REFERENCE.md:157",
      "must_be_absent_from_source": ["3001"],
      "task": "Our Grafana is on a non-standard port. Move it to the upstream default 3000 so it matches every tutorial.",
      "discriminator": "Does the session find and state the recorded reason for 3001 before proposing a change? Silently moving it to 3000 is the miss."
    }
  ],
  "retired": [
    {
      "probe_id": "P01-reaper-protection-keys",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- the fact is in the resident '## Hazards' list of AGENTS.md ('run-dir *.pid', 'run root'), which protocol section 4 explicitly excludes from being an occasion. Tested nothing about pointer-following."
    },
    {
      "probe_id": "P03-ecosystem-repos-lockstep",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- ECOSYSTEM_REPOS is still resident in AGENTS.md. Run scored miss/source-recovered; observation invalidated in the ledger."
    },
    {
      "probe_id": "P04-default-repos-orthogonal",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- DEFAULT_REPOS is still resident in AGENTS.md. Run not recorded. It did surface a real defect: juniper-recurrence ships no claude.yml, so the weekly audit silently covers 8 of 9 repos and exits 0."
    },
    {
      "probe_id": "P05-security-scan-asymmetry",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- 'Do not copy either contract onto the other path' is still resident in AGENTS.md. Run not recorded. It did surface a real defect: both pip-audit jobs audit an empty dependency set."
    },
    {
      "probe_id": "P09-agents-md-already-today",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- the already-today escape hatch is still resident in AGENTS.md. Run not recorded. It did surface that AGENTS.md's stacked-PR remedy is backwards on durability."
    },
    {
      "probe_id": "P10-publish-gate1-extras",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- '--no-deps' is still resident in AGENTS.md. Run not recorded. It did surface that 79% of the TestPyPI verify step is one unconditional sleep 30."
    },
    {
      "probe_id": "P11-sibling-publishers-release-only",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- 'push: tags' and the double-fire rationale are still resident in AGENTS.md. Run scored miss/source-recovered; observation invalidated in the ledger."
    },
    {
      "probe_id": "P12-subpackage-ci-path-filters",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- 'path filters must include' is still resident in AGENTS.md. Run scored miss/source-recovered; observation invalidated in the ledger."
    },
    {
      "probe_id": "P13-main-verify-catchup-base",
      "retired_on": "2026-08-21",
      "reason": "INVALID -- the G3.1 catch-up base definition is still resident in AGENTS.md. Run not recorded. It did surface a confirmed defect: the base ratchets on GREEN rather than on SCREENED, so each red guarantees the next."
    }
  ]
}
