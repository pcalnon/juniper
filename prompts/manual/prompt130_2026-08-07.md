# Remaining work

## juniper-ml PR issues

### Session Info

claude --resume "juniper-ml PR issues handling"

### Outstanding Tasks

- #998 stays open by design (not auto-closed on green) — once #1000 merges and main-verify greens, close it after adjudication.
- PR #929 (fleet YAML rehearsal of the same notify) — triage against the now-merged structural guard: likely complementary (rehearsal vs. structural), possibly partial-dup.
- #887/#888 legacy per-SHA trackers still open.

## ci make green

### Session Info

claude --worktree soft-scribbling-sphinx --resume "ci make green"

### Outstanding Tasks

- ml main is currently red (since yesterday ~21:46, the #931/#932 fleet-docs merges): pre-commit black + bandit failures on the push lane, plus a Main-Verification failure on #931.
  - An active session already owns the heal — locked worktree on fix/main-ci-red-heal with PR #1000 open.
  - I stayed out to avoid a collision.
- The cascor PR storm (30 open) remains the other session's; ml is also at 30 open PRs — exactly the PR Budget ALARM threshold, worth a look once #1000 lands.
- The primary ml checkout's local main has diverged from origin/main — the F-6 guard held, so I left it exactly as found (likely another session's in-flight state).
- One stale centralized worktree predating this session (juniper-cascor--fix--uvicorn-access-log-…--20260711) — cascor turf, flagged only.

---
